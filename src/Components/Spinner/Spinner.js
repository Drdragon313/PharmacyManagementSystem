import React from "react";

import "./Spinner.css";
const Spinner = () => {
  return (
    <div className="loader">
      <div className="spinner"></div>
      Loading...
    </div>
  );
};

export default Spinner;
